# 本轮验收

日期：2026-09-13。最终离线 HTML SHA-256：`495e57fa1ca105e8a52bf3e196e37e729fd798e6dc06dbfd33d1fb4a57028829`。

- 全套既有回归＋洪水新回归＋类型检查通过，详情 `regression.json`。
- `vinext build` 完成，存在原有大 chunk 与路由静态分类提示，没有构建错误。依赖缓存位于共享 node_modules，构建使用获准的缓存写入。
- 原生 Canvas 测试检查玻璃多边形并集、矩形相交和场景切换；遮罩外新增雨/水像素为零。见 `generic-presentation-report.json`。
- 原生 Edge 隔离浏览器：320×568、375×667、390×844、430×932、1440×900，26 项检查通过；全六项真实输入通关、不同操作顺序、错误反馈、暂停、静音、重玩奖励去重均通过。没有注入关卡完成状态，测试预置只用于地图前置成绩。
- 零 pageerror、零 HTTP(S) 请求，浏览器关闭已验证。音频 API 未被替换；在原生 BufferSource.start 上记录三条实际循环以及动作短音启动。
- 主审阅已查看上楼、通话、挥衣、最终待援截图，及暖底/深底人物透明图。最终待援人物和水瓶分开，衣物没有独立悬空副本。
- 完整源码中的共享 viewport/camera/CSS、关卡 level.json、地图解锁配置以及存档格式均未修改。
- **未完成项**：物理手机真机、扬声器/耳机人工听音。因此不声称音量主观体验或低端真机性能已验收。

同步时以 SHA 基线逐项检查正式源码及用户原离线文件，备份后仅复制本轮文件。实际文件交付后还需执行 `test-flood-refinement-browser.mjs <实际HTML路径> <独立报告目录>`，报告写到本地交付目录，不覆盖源文件验收。
