# 2026-09-11 地图与种花闭环验收

最终测试对象为本轮导出的独立离线 HTML，95,379,561 字节，SHA-256：

`cbe20eb9c6f83d822672b9f224002786760145fedf1c5dafc27c9490fb682cd7`

## 视觉调整

五类栏目采用 [国家应急广播·科普动画](https://www.cneb.gov.cn/kepu/kepudonghua/) 的名称与顺序。公共卫生、社会安全暂未制作关卡，明确显示待开放；已验证点击仅打开说明，不发奖、不改进度、不获得空奖章。栏目、地图关联与状态从独立配置读取。

- 三张独立插画地图，通过底部分类导航切换；自然灾害有 8 关，事故灾难与生活安全各 4 关。
- 区域名与品牌处在同一条顶部栏，去掉第二排大标题。
- 从下方当前关卡出发，完成后向上引导下一关，不自动进入。
- 重画自然、公共地图，使住宅、物资站、路口、公园、林缘、电梯和井口成为真实地物。保留关卡原名，没有通过改名掩盖地点不对应。
- 花苞重新生成透明插画：收尖花瓣、叶片、短茎与草簇；去掉旧圆球花苞和圆形数字底牌。完成后显示开放主花。
- 节点坐标固定在花坛接地处，标签独立放在下方。井口标记在围挡旁安全路面，电梯标记在入口，厨房标记在住宅门前。三张图均已在实际浏览器目视检查。
- 名称、图片、坐标、动画时序分别配置；换图或改显示名称不修改存档 ID、目标蒙版或关内规则。

## 自动化结果

| 检查 | 结果与证据 |
| --- | --- |
| 既有规则与新增旅程单元检查 | 12 套、1822 项通过；见 `verification/suite-report.json` 与日志。新增旅程模块 17 项包括首次领取、旧档、玩家隔离、素材、向上路线与五类配置。 |
| TypeScript | `tsc --noEmit` 通过。 |
| 生产构建 | `vinext build` 完成全部五阶段。该版本构建器仍提示路由静态分类为 Unknown，未阻断构建。 |
| 新增地图 UI 与测试脚本静态检查 | 目标文件 `oxlint` 通过。全仓库静态检查未列为通过项；既有播放器和排行榜模块仍有 Hooks、语义标签及控制字符正则等 lint 报告。 |
| 离线导出 | 导出器的 7 项自检通过，图片、音频与代码内嵌。 |
| 16 关真实操作 | `test-journey-browser.mjs` 通过；隔离浏览器以真实指针完成目标，未注入花数或完成标记。 |
| 奖励闭环 | 首次完成 +3，安全结果、结算、返图种花、钱包、节点状态与下一关解锁通过。完成 16 关后刷新仍是 48 花、16 个完成节点。 |
| 不应奖励的情况 | 重玩、未完成离开、计时失败均不额外奖励；未解锁节点不可进入；失败不提前解锁。 |
| 玩家与存档 | 新玩家为独立 0 花；切回原玩家保留 48 花；减少动态效果下种花途中刷新仍保留已提交奖励。 |
| 长期反馈 | 三个区域全部恢复、区域完成标记与档案奖章通过，刷新保留。 |
| 地图竖屏与桌面 | 320×568、375×667、390×844、430×932、540×960、1440×900；无页面溢出，顶部文字不撞按钮，初始节点露在导航上方，导航按钮至少 44px。 |
| 结算竖屏 | 上述前五种尺寸均通过按钮、边界与可见性检查；安全场景继续留在背景。 |
| 原找隐患视口回归 | 9 关 × 7 尺寸，共 63 组布局检查；9 关真实指针通关与重开通过。此脚本只为测布局预置解锁，发奖行为由上面的无注入闭环脚本单独验证。见 `verification/hunt-layout/report.json`。 |
| 浏览器错误与外部依赖 | 两套离线浏览器测试均无页面脚本错误、无 HTTP(S) 请求；测试浏览器已关闭。 |

实体手机触摸体验与人工听音未执行；浏览器测试固定静音，不能替代人工音量与音色验收。

## 交付位置与保全

维护源码：`D:/中关村/小红花/小红花游戏`。

用户离线文件：`D:/中关村/小红花/test1/outputs/本地离线版/小红花应急行动.html`；维护源码目录内同名离线文件同步为相同内容。

交付脚本只同步本任务文件，逐项检查与初始基线一致，写入前备份并在写入后校验 SHA。同步清单、原文件备份与最终回执保存在 `D:/中关村/小红花/test1/work/flower-journey-delivery-*.json` 及同目录带时间的备份文件夹。既有历史验证文件、退役地图、一次性编辑脚本不进入维护源码。

日后修改图片、名称和落点请从 [模块说明](README.md) 开始。
