# 本轮验收记录

日期：2026-09-10。范围：厨房处置关声画增强、非语言音频、公共火灾处置模板、批产文档。最终规则以用户“去掉全部 AI 朗读”的要求为准。

## 自动检查

- 完整回归：1,695 项测试，0 失败。分组：厨房51、声画14、台风28、配置流水线48、榜单29、四关188、场景找隐患276、性能138、批次892、暴雨洪水31。
- 隔离批产冒烟：创建两个处置草稿，继承压力/人物/非语言声音；默认禁用；重复 ID 和未审核启用被拒绝。未向正式关卡清单增加测试关。
- TypeScript：`tsc --noEmit --incremental false` 通过。
- 生产构建：Vinext 的 client / RSC / SSR 全部成功；日志保留构建器现有的路由静态分类提示，不将其当作运行错误。
- 关卡配置/美术：6 个配置组合的规则、可达性、本地资源与透明通道检查通过；原有启用关卡保留16关。
- 音频：18 个 PCM16/22050 Hz 非语言文件，通过实际文件能量、峰值、文件存在检查；没有朗读文件。6条常驻循环、最多6个同时播放的一次性音源。
- 离线导出：72,805,855 字节；SHA-256 `ef70307bd0e14a6c2642a77cdc7ed1dc3d854f4374e7e0c11220bf1666ee2e1c`。18音频内嵌、voice-/tts- 文件0、无运行时文字朗读API、网络策略 `connect-src none`。

## 实际浏览器观察

- 在390×844手机视口检查低/高风险、错误泼水、火势抬升、人物举手受惊；缩小后的人物动作未切掉手脚。另在默认桌面窗口进行完整交互。
- 无朗读版本完整流程：错误泼水 → 点击关燃气 → 点选锅盖盖锅 → 点选人物到门外 → 正常显示“厨房安全了”，奖励2朵。火焰消失、黑烟退去、人物在门内，色调恢复。
- 满风险后仍能关火、覆盖；静音、暂停、重玩可用。暂停数据 `state=suspended`、`RMS=0.00000`；重玩后循环数仍为6。恢复且播放时测得非零RMS（一次采样0.10122），素材加载18、缺失0。采样并非响度标准或主观试听。
- 导出 HTML 通过本机 HTTP 原样提供，实际进入厨房并显示完整内嵌场景。该72.8MB单文件超过了当前自动化工具的完整页面快照传输上限，因此使用截图确认，不声称完成了该单文件的全部自动化 DOM 检查。
- 已使用的参考：提供的视频约40.60秒，抽帧观察人物、场景明暗和动作状态。参考HTML的本地浏览器打开被策略阻止；没有绕过、执行混淆代码或复用素材。

## 明确保留的未验证项

- 本轮没有实体手机、Safari、实际断网 `file://` 双击逐一测试；HTTP承载内嵌HTML及结构验证不等于这些测试。
- 没有将波形检查标成“人工听感验收”。商用前仍需手机外放/耳机听感、长时间循环、音量疲劳和内容安全复核。
- 人物为状态图与程序姿态动画，不是完整骨骼动作或重新生成的视频角色。
- 蒙版法、生图提示词是后续制作规程，本轮没有重做图片或调用AI声音生成。

中途曾发现测试暂存目录缺少原画加工记录，补上只读原画目录后完整回归通过；没有为通过测试更改旧关卡规则。测试脚本生成的旧批次报告没有覆盖原仓库版本。
