# 本机排行榜验收记录

日期：2026-09-09。范围：排行榜新增、两类关卡奖励接入、本地存档迁移与离线导出。未修改关卡机制、人物、美术或动画。

## 自动检查

- 厨房原有测试 51 项、台风原有测试 28 项、流水线原有测试 48 项、新排行榜及打包隔离测试 29 项，合计 **156 项全部通过**。
- TypeScript `tsc --noEmit` 通过。
- 完整源码目录最初把 `outputs/历史备份` 下的旧 TS 文件也纳入检查；已在 `tsconfig.json` 排除 `outputs`、`work`、`dist`，不删除备份，并增加自动测试防止回归。
- vinext 生产构建通过。构建工具仍提示静态分析无法分类 `/` 路由，此提示不阻止构建完成。
- 离线导出通过脚本语法、无外部模块/样式导入、图片内嵌、单脚本及无外部资源引用检查；新增检查确认排行榜脚本和 CSS 都在离线文件中。
- 离线文件 13,600,683 字节，SHA-256：`cc6a3300ccd722a508f4490e6568bd7a74c67806fa7bf778f7ab0cfe09a9b6d6`。
- 临时工作目录的 node_modules 是依赖目录联接，pnpm 的自动依赖检查拒绝跨目录安装。没有重新安装/移除依赖；使用现有 Node 依赖直接执行相同测试、构建和导出脚本，均通过。

## 浏览器实际操作

1. 当前 `http://localhost:3002/` 的旧存档总计 5 朵，升级后迁入“小红花玩家”，榜单显示第 1 名、5 朵。未改动该玩家昵称或地区。
2. 独立测试地址 `http://[::1]:3002/` 首次只创建一个零分玩家，没有虚构竞争者。
3. 将测试玩家资料改为“小花甲 / 山东 · 青岛”，显示正确。
4. 新建“小花乙 / 浙江 · 杭州”，自动切换且成绩独立从零开始。
5. 用小花乙实际进入厨房关卡，点击关火、拖动锅盖，最后通过键盘辅助目标完成撤离；结算显示获得 3 朵。
6. 返回排行榜显示小花乙第 1 名、3 朵、浙江 · 杭州；小花甲 0 朵、未上榜、山东 · 青岛。
7. 切回小花甲，再刷新并重新打开榜单，两人的昵称、地区、成绩及当前玩家选择仍保留。
8. 390×844 视口下内容宽度 375px；320×740 视口下内容宽度 305px（均扣除浏览器滚动条）。document scrollWidth 等于 clientWidth，无横向溢出，列表可读。桌面默认视口下正常展示竖屏布局。
9. 测试标签的浏览器错误/警告日志为空。临时测试标签已关闭，未将测试玩家写入 `localhost:3002` 的用户存档。

## 交付与边界

完整源码已同步到 `D:\中关村\小红花\小红花游戏`。同一离线导出已同步到该目录及 `D:\中关村\小红花\test1` 下的 `outputs\本地离线版`，逐文件哈希校验一致。

被覆盖的原文件备份在 `D:\中关村\小红花\小红花游戏\outputs\历史备份\排行榜前-20260909-112715`。未提交或推送 GitHub。

尚未验证：实体手机、所有浏览器的 file:// 存储差异、两个窗口同一时刻并发写入、真正公共服务器。离线文件本次只做自包含导出检查，未通过浏览器自动化直接打开 file://。旧游戏关卡机制未因此新增回归修复。
