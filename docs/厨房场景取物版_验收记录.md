# 厨房场景取物版 · 2026-09-05

## 本次范围

只升级第二关《厨房着火了》的场景、美术布局和输入映射，保持台风关原样。不新增关卡，不改变油锅处置规则、奖励和存档键。无工具栏的场景取物仍允许折叠的键盘辅助操作。

## 交付内容

- 生活化厨房背景 `public/levels/kitchen-v1/room-integrated-v2.png`，内置 imagegen 原创生成，风格库约束暖色纸纹插画与厨房空间关系。
- 六个可操作物品使用原有真正透明 PNG，放置、归位和命中区域在 `app/game/kitchen/config.ts` 独立配置。
- 拖拽保持抓取偏移，错误物品归位，盖好锅盖后不留原位副本，人物拖拽无复制影像。
- 720×960 完整竖屏厨房，原底部工具架已移除。燃气旋钮保持点击旋转操作。
- 独立单文件离线 HTML，保留两个关卡。

## 已执行检查

1. `node scripts/test-kitchen.mjs`：42 项全部通过。
2. `node scripts/test-typhoon.mjs`：28 项全部通过。
3. `node scripts/check-kitchen-art.mjs`：13 张交互和人物素材均是真正 RGBA，外边缘透明。
4. `tsc --noEmit`：通过。
5. `vinext build`：通过；开发构建缓存需要正常目录写权限。隔离工作目录引用共享依赖时先遇到 EPERM，授权临时缓存写入后构建成功。
6. `node scripts/export-offline.mjs`：六项导出检查通过；内嵌所有图片、脚本和样式，无外部模块依赖，connect-src 为 none。
7. 浏览器 390×844 实际点击与拖动：六物件均可从场景操作；水、抹布造成错误反馈及惊慌表情；灭火器偏移提示；餐盘、菜刀弹回；满风险仍能通关；关火后盖盖和先盖后关都完成；锅盖从备菜台消失，人物最终进入门内；纠错 2 花、无危险动作 3 花、重玩恢复场景。

## 未作承诺

没有执行实体 iOS / Android 设备测试。浏览器通过 localhost 检查单文件；不以 HTTP 检查冒充 file:// 协议验收。没有新增联网依赖，没有在本次修改中自动推送公开仓库或更新线上发布。最终消防知识仍建议由专业人员审核。

## 回归步骤

重新导出后打开单文件，开始厨房关；从前景台拖动水杯到锅上确认错误；点击旋钮关火，拖锅盖盖锅，拖人物进入门洞。重玩后先点锅盖再点锅，再点旋钮标签并撤离，检查另一顺序和 3 花。查看备菜台和门边物件是否归位，确认没有底部工具栏。修改任何物件坐标后重复手机尺寸测试，不能只通过状态机测试就发布。
