# 首批文字稿关卡交付与验收

日期：2026-09-09。源码基线：`8225493da7b99fdb0eef264613f0d36ab81c56a7`。

## 本次交付

已对 53 篇稿件完成主题筛选与逐篇设计去留记录，编号缺 14 是原包情况，不补造稿件。全量记录见本目录 Markdown 与 JSON。只有首批四关制作并启用，不把候选方向当成已完成关卡。

| ID / 编号 | 名称 | 操作与收尾 |
| --- | --- | --- |
| flood-kit / 09 | 汛期应急包 | 点击五种物资，逐件收入包中；补充家庭需求、不耽误转移 |
| clear-corridor / 10 | 畅通的生命通道 | 点击四件杂物，收进自家储物间；出口和楼道保持清空 |
| lift-wait / 11 | 电梯停住了 | 报警通话、人物移到轿厢内远离门边；门仍关闭，等待救援 |
| well-call / 12 | 井口边的求助 | 旁观者后退，模拟联系 119 和 120；井口仍危险，留在外围 |

原两关和本地多玩家排行榜保留。03—08 为旧草稿/模板保留编号，不代表被删除的关卡。新四关均无虚构的灾害或救援倒数；正确步骤不强制无必要顺序，错误后可以完成。

## 模块与美术

规则在各关 `level.json`，独立皮肤在 `skins/paperbook.json`。图片替换、摆位、图层、关键帧和表情状态不写进业务引擎。四关共享 `configured-v1`，没有按这四个名称分支硬编码。

使用 imagegen 技能生成四幅原创暖色纸纹绘本背景和四张物件/人物图集；原图与提示词保存在 `art-source/transcript-v1/`。按既有授权机械去背、去污染边缘、切分透明 PNG；金属面板使用轮廓遮罩保留内部浅色。水瓶尺度、收音机接地、手机文字位置和小屏报警面板已校准。运行素材共 24 个文件，约 3.91 MB。

素材生成与透明通道检查不等于独立法律鉴定。没有复制第三方游戏画面、原视频素材或真实机构背书。

## 自动化结果

- 厨房回归：51/51。
- 台风回归：28/28。
- 通用流水线：48/48。
- 本机排行榜：29/29。
- 首批文字稿关卡：188/188。
- 合计：344 项通过。新关覆盖 152 种合法动作排列、误操作无目标奖励且可恢复、重复输入、500—1500 ms 处理动画、完成帧才提交、末帧连续、换皮规则轨迹一致、20 分钟等待不制造灾害、320/390 坐标映射、待援结束危险保留。
- TypeScript 类型检查通过；生产构建通过。构建工具提示静态分析无法分类 `/` 路由，这不是构建失败，也不等于已验证所有部署平台。
- 离线导出六项自包含检查通过：JS 语法、无模块依赖、无 CSS 外链、图片内嵌、单脚本、无外部 HTML 资源。CSP 为 `connect-src 'none'`。

## 实际浏览器检查及限制

通过本次独立本地开发页进行实际点击与拖放，并检查 390×844、320×740 竖屏下的画面。楼道四物件完成并发放三朵花；井口误点触发表情/危险提示，人物后退及两个模拟电话后以两朵花结算，井口没有消失；电梯误点门后报警并拖动乘客，在轿厢内以待援结算，门未打开。重玩清除本局目标，不删除历史最佳成绩。应急包在独立测试页实际点击五件物品，逐件消失、剪影完成并发放三朵花，结算为“准备好了”。

面板字号与电话对齐已根据小屏截图修正。只在物件实际轮廓命中；细杆之间的透明空隙不会被误判成物件。实际检查重玩后目标清空、暂停菜单可用。保留暂停与键盘辅助输入，不用巨大常驻答案条遮挡场景。

本轮没有实体手机测试；没有声称所有浏览器均已验收。浏览器工具对 `file://` 的打开受限，因此离线版完成的是打包、语法和无外部依赖检查，不冒充直接双击试玩已验证。请在实际投放设备上再次双击离线文件验收。没有修改用户原浏览器端口的存档。

## 使用与继续生产

完整源码以 `D:\中关村\小红花\小红花游戏` 为正式目录；本轮工作副本在 `test1/work/transcript-levels-v1/game`。离线交付同时更新正式目录和原先熟悉的 `test1/outputs/本地离线版`，更新前备份被替换文件，哈希记录在工作目录的 `delivery-receipt.json`。

```powershell
pnpm install
pnpm dev
pnpm test
pnpm typecheck
pnpm build
pnpm export:offline
```

后续从审查清单挑选候选主题，先补足直接适用的权威依据、条件和专项复核，再做分层美术与 JSON 包；新包默认隐藏，验收后启用。医护、复杂逃生等专项复核项不能直接批量开放。内容经过同一 AI 的两轮复核，不是独立专业审核、医疗建议或商用认证。

本轮仅本地交付，没有提交、推送或发布 GitHub，也没有重新分发原始文字稿 ZIP。
